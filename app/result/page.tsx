'use client';
import { useRouter } from 'next/navigation';

function QuizResult() {
  const router = useRouter();

  return (
    <div>
      <h1>QuizResult</h1>
    </div>
  );
}

export default QuizResult;
